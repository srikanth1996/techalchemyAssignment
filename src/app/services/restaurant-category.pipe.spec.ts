import { RestaurantCategoryPipe } from './restaurant-category.pipe';

describe('RestaurantCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new RestaurantCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
